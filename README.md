# voady_code_case_api
Code case for Voady - a hair product inventory.

## To run the cloned server locally
`$ yarn`

`$ npx sequelize db:create`

`$ npx sequelize db:migrate`

`$ npx sequelize db:seed:all`

`$ yarn start`
